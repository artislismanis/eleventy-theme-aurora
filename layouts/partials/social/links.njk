{#
  Social links partial
  Iterates through theme.social and renders icons
  Supports both account-based URLs and custom URLs
#}
{% if theme and theme.social and theme.social.length %}
  <nav class="social-links" aria-label="Social media links">
    {% for social in theme.social %}
      <a
        href="{{ social | socialUrl(theme.socialPlatforms) | escapeAttr }}"
        class="social-link social-link--{{ social.platform | escapeAttr }}"
        rel="me noopener noreferrer"
        target="_blank"
        aria-label="{{ social | socialLabel | escapeAttr }}"
      >
        {% include "partials/social/icon.njk" %}
      </a>
    {% endfor %}
  </nav>
{% endif %}
