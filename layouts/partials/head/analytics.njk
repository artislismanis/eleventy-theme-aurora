{#
  Analytics partial
  Supports Google Analytics and Plausible
#}
{% if site and site.analytics %}
  {# Google Analytics #}
  {% if site.analytics.googleAnalytics %}
    <script
      async
      src="https://www.googletagmanager.com/gtag/js?id={{ site.analytics.googleAnalytics | escapeAttr }}"
    ></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag('js', new Date());
      gtag('config', '{{ site.analytics.googleAnalytics | escapeJsString }}');
    </script>
  {% endif %}

  {# Plausible Analytics #}
  {% if site.analytics.plausible %}
    <script
      defer
      data-domain="{{ site.analytics.plausible | escapeAttr }}"
      src="https://plausible.io/js/script.js"
    ></script>
  {% endif %}
{% endif %}
